const app_config = {
    backend_url: "http://localhost:5001",
  };
  
  export default app_config;


// confif file is use for good parctice because no need to required to change everywhere the url just change config url,port,bd_url, its done.